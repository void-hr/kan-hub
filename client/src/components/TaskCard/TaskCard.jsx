import styles from './taskcard.module.css'

const TaskCard = ({title, priority, checklist, dueDate}) => {
  return (
    <div className={styles.container}>
        TaskCard
    </div>
  )
}

export default TaskCard